import { StoryCardPipe } from './story-card.pipe';

describe('StoryCardPipe', () => {
  it('create an instance', () => {
    const pipe = new StoryCardPipe();
    expect(pipe).toBeTruthy();
  });
});
