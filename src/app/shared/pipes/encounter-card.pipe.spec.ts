import { EncounterCardPipe } from './encounter-card.pipe';

describe('EncounterCardPipe', () => {
  it('create an instance', () => {
    const pipe = new EncounterCardPipe();
    expect(pipe).toBeTruthy();
  });
});
